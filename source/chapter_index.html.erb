---
title: Local Chapters
---
<%
chapter_weights = data.chapters.keys.map do |chapter|
c = data.chapters[chapter]
unless data[chapter].nil?
  c["weight"] = data[chapter].keys.select {|i| /[0-9]/.match(i) }.count
else
  c["weight"] = 0
end
c["id"] = chapter
if chapter == "seoul"
  c["weight"] = 0
end
c
end
%>
<article class="chapter">
  <section class="chapters chapter-index">
    <h1>Papers We Love Chapters</h1>
    <p>Join the community by finding a chapter near you, or maybe start your own!</p>
    <ul class="chapter-index-list">
      <% chapter_weights.reject {|i| i.nil? }.sort_by! {|i| i.weight}.reverse().each do |chapter| %>
        <li><%= link_to chapter['title'], "/chapter/#{chapter['id']}", :title => chapter['title'] %></li>
      <% end %>
    </ul>
  </section>
</article>
